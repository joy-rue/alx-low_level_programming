#include main.h

/**
 * _islower - checks if the given letter is lower case
 *
 *  Return: 1 if true and 0 if the letter is not lowercase
 */
int _islower(int c)
{
	if (c >= 'a')
	{
		return (1);
	}
	return (0);
}
